# Kavach AI Security - Environment Configuration Example
# Replace this placeholder with your actual OpenAI API key
# You can obtain an API key at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# Backend server configuration
PORT=5000
HOST=localhost
DEBUG=False

# Security settings
# Generate a secure random key using a tool like:
# python -c "import secrets; print(secrets.token_hex(16))"
ENCRYPTION_KEY=your-secure-encryption-key
SSL_VERIFICATION=True

# Note: For testing purposes, you can leave these values as they are.
# The application will use fallback mechanisms if the OpenAI API key is not provided.
# However, for full functionality, please provide a valid OpenAI API key. 